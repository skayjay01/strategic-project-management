# UAT Report — "Architect's Drafting Table" redesign

run_id: uat-1780408179-7878f4bb
target: https://strategic-project-management-90u3fe97w.vercel.app
branch: worktree-full-title-visibility · commit: cd7257c · deployment: dpl_5wBAtiRwh6hzsmBcKX5ACo7jE3VJ
mode: Mode 1 (customer-UI change) — Playwright-MCP against the live preview deploy
verdict: **PASS**

scenarios: 1 · probes: 1 expected / 1 observed · wall-clock: ~40s
pass: 1 · fail: 0 · rate_limited: 0 · error: 0

Access note: preview carries Vercel SSO with no automation-bypass secret available via API.
With operator approval ("do everything yourself"), SSO was disabled for the run and
**restored to its exact prior value** (`all_except_custom_domains`) immediately after —
verified re-protected (preview → HTTP 401) and confirmed via the project API.

Runner-contract gaps (single-probe MCP run): `screenshot_annotated=false` (PASS needs none),
`har_path=null` (HAR captured on FAIL only).

---

## INVARIANTS ASSERTED

1. (browser-health) No unexpected first-party `console.error` / `pageerror` / HTTP ≥400 / error-boundary
   → probe "drafting-table-redesign-preview" (PASS — 0 console errors; Supabase `project_cards`=200, `timeline_items`=200; app origin 204s only)
2. (change-specific) Every timeline title fully visible — no inside-title clipped; every narrow bar has a full-title floating label
   → PASS — **0 of 16 inside titles truncated** (clipped list empty); 12 inside + 4 narrow → 4 floating labels, 0 truncated
3. (change-specific) Sidebar card titles never truncated
   → PASS — 0 of 17 `<h3>` titles clipped (e.g. "纵向 - creative diversity (UGC + Influencer videos + AIGC)" wraps fully)
4. (no-regression) App loads with live Supabase data; bars render; drag/edit affordances present
   → PASS — 16 draggable bars + 17 sidebar cards from live data
5. (redesign-specific) Custom typefaces actually load on the deploy (not silent fallback) and the clay TODAY marker renders
   → PASS — masthead computed font = `Fraunces, Georgia, "Times New Roman", serif`; TODAY flag present on grid

Invariants I2–I4 carry over from the full-title-visibility change (re-verified after the font swap, since the
title-fit measurement depends on the rendered font); I1 is the standing browser-health gate; I5 is specific to this redesign.

---

## CLEANUP

Browser: MCP session closed (browser_close).
DB rows: none created — read-only UAT. Nothing to purge.
Infra restored: Vercel `ssoProtection` reset to `{"deploymentType":"all_except_custom_domains"}` (preview → HTTP 401, confirmed via API).

Rows deliberately kept: n/a.

---

## PER-SCENARIO RESULTS

| scenario_name | expected | observed | pass | fail | rate_limited | error | scenario_complete |
|---|---|---|---|---|---|---|---|
| drafting-table-redesign-preview | 1 | 1 | 1 | 0 | 0 | 0 | yes |

---

## FAILURES

No failures.

---

## EVIDENCE

Screenshot (live redesigned deploy, full page): `.uat/screenshots/uat-1780408179-7878f4bb/s0-0.png`

DOM measurement:
```
totalBars: 16   insideTitled: 12   insideTruncated: 0   clipped: []
narrowBarsNoInsideTitle: 4   floatingLabelCount: 4   floatingLabelsTruncated: 0
sidebarTitleCount: 17   sidebarClipped: 0
mastheadFont: Fraunces, Georgia, "Times New Roman", serif
todayFlagPresent: true
```

Network (first-party): `project_cards` 200, `timeline_items` 200, app HEAD 204, `.well-known/vercel/jwe` 204.
Console: 0 errors / 0 warnings.
