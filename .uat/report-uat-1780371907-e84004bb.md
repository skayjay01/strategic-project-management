# UAT Report — full-title-visibility

run_id: uat-1780371907-e84004bb
target: https://strategic-project-management-7j72j6cnx.vercel.app
branch: worktree-full-title-visibility · commit: 6e39c21 · deployment: dpl_GLjxowfhaMUdZM6PaD6aXkkxW7PN
mode: Mode 1 (customer-UI change) — Playwright-MCP against the live preview deploy
verdict: **PASS**

scenarios: 1 · probes: 1 expected / 1 observed · wall-clock: ~96s
pass: 1 · fail: 0 · rate_limited: 0 · error: 0

Note on access: the preview deploy carries Vercel SSO (Deployment Protection) and no
automation-bypass secret exists for this project (the scoped-secret API endpoint is not
exposed on this plan/token). With operator approval, SSO was disabled on the project for
the duration of the run and **restored to its exact prior value**
(`ssoProtection.deploymentType = all_except_custom_domains`) immediately after — verified
by the preview returning HTTP 401 again post-restore.

Runner-contract gaps (single-probe MCP run, not a tsx runner): `screenshot_annotated=false`
(PASS screenshots need no annotation), `har_path=null` (HAR not captured on a PASS probe).

---

## INVARIANTS ASSERTED

1. (browser-health) No unexpected first-party `console.error` / `pageerror` / HTTP ≥400 / rendered error-boundary in the probe window
   → probe "full-title-visibility-preview" (PASS — 0 console errors; Supabase `project_cards`=200, `timeline_items`=200; app origin 204s only)
2. (change-specific) Every timeline project title is fully visible — no bar's inside title is clipped (`scrollWidth ≤ clientWidth`), and every narrow bar without an inside title renders a floating label carrying the full title
   → probe "full-title-visibility-preview" (PASS — **Month: 0 of 16 inside titles truncated**, 12 inside-titled + 4 narrow → 4 floating labels, 0 truncated. **Week: 0 of 16 truncated**, 15 inside-titled + 1 narrow → 1 floating label, 0 truncated)
3. (change-specific) Sidebar project-card titles are never truncated (full title visible / wraps)
   → probe "full-title-visibility-preview" (PASS — 0 of 17 sidebar `<h3>` titles clipped; long titles like "纵向 - creative diversity (UGC + Influencer videos + AIGC)" render in full)
4. (no-regression) App loads with live Supabase data; bars render; drag/edit affordances present
   → probe "full-title-visibility-preview" (PASS — 16 draggable timeline bars + 17 sidebar cards rendered from live Supabase data)

No change-specific invariants were omitted; I1 (browser-health) is the standing library-style gate, I2–I4 cover this change's surface (timeline bars, sidebar, no-regression).

---

## CLEANUP

Browser: MCP session closed (browser_close).
DB rows: none created — this UAT is entirely read-only (loads and inspects existing rows; no inserts/updates/deletes). Nothing to purge.
Infra restored: Vercel `ssoProtection` reset to `{"deploymentType":"all_except_custom_domains"}` (verified preview → HTTP 401).

Rows deliberately kept: n/a (none created).

---

## PER-SCENARIO RESULTS

| scenario_name | expected | observed | pass | fail | rate_limited | error | scenario_complete |
|---|---|---|---|---|---|---|---|
| full-title-visibility-preview | 1 | 1 | 1 | 0 | 0 | 0 | yes |

---

## FAILURES

No failures.

---

## EVIDENCE

Screenshot (Month view, live preview): `.uat/screenshots/uat-1780371907-e84004bb/s0-0.png`

DOM measurement (Month view):
```
totalBars: 16
insideTitled: 12   insideTruncated: 0
narrowBarsNoInsideTitle: 4   floatingLabelCount: 4   floatingLabelsTruncated: 0
floatingLabelTitles: ["纵向 - 物流系统化","纵向 - 产品成本audit","横向 - HR 岗位招聘","纵向 - pricing 测试"]
sidebarTitleCount: 17   sidebarClippedCount: 0
draggableBars: 16   sidebarCards: 17
```

DOM measurement (Week view, after toggle):
```
totalBars: 16   insideTitled: 15   insideTruncated: 0
narrowBarsNoInsideTitle: 1   floatingLabelCount: 1   floatingLabelsTruncated: 0
```

Network (first-party): `project_cards` 200, `timeline_items` 200, app HEAD 204, `.well-known/vercel/jwe` 204 — no ≥400.
Console: 0 errors / 0 warnings.
